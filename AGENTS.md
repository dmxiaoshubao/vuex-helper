# Agent Working Rules

1. 在处理任何需求前，先阅读：
   - `docs/quality/engineering-rules.md`
   - `docs/quality/performance-baseline.md`
2. 默认按上述规则执行（规范、性能、缓存、复用、测试）。
3. 除非用户明确要求，不要把开发规则写入面向用户的文档（如 `README.md`）。
